DB_USER=root
DB_PASSWORD=password
DB_NAME=db

DATABASE_URL=postgresql+asyncpg://root:password@localhost:5432/db

BACKEND_URL="http://localhost:8000"

ALLOWED_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173","http://localhost:3000","http://127.0.0.1:3000"]

JWT_SECRET_KEY=
JWT_ALGORITHM=HS256
# jwt cookies secure is HTTPS only (set false for local environment)
JWT_COOKIE_SECURE=False 

# Mail (SMTP) — MailHog local dev catcher, no auth. UI at http://localhost:8025
MAIL_USERNAME=""
MAIL_PASSWORD=""
MAIL_FROM=no-reply@example.com
MAIL_FROM_NAME=Fasti Kit
MAIL_PORT=1025
MAIL_SERVER=localhost
MAIL_STARTTLS=False
MAIL_SSL_TLS=False
